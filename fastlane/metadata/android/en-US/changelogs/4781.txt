- Agent/MCP: the reverse tunnel that exposes Haven's MCP server to a designated SSH host now stays up while WireGuard is in use. Previously enabling WireGuard tore it down, which broke MCP access on a remote endpoint host (only the device's own WireGuard clients were served).
- Agent: present_media now references a file — on the device, in the Linux guest, or on an SFTP/SMB/rclone connection (or a URL) — instead of carrying the image/audio inline. Lighter, and works for larger media.
- Agent: new present_web shows HTML, an SVG, or a PDF inline in an in-app viewer — a step between a static image and a full remote-desktop app window.

Fixes a colour-scheme glitch from 5.59.78: returning to Haven by tapping the agent-approval notification now keeps your Material You theme, instead of briefly falling back to the default palette. The notification wording is also clearer — when an agent (MCP) action is blocked because Haven is in the background, bring Haven to the foreground and the agent can retry, so you can allow or deny it then. The action still never proceeds without your explicit OK.

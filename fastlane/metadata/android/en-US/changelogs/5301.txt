Linux desktop reliability: a desktop that only half-installs now fails with the exact missing components named — and repairs itself on the next open — instead of "installing" and then showing a blank screen. The Wayland screen-capture shim now loads on older-glibc distros like Void. Behind the scenes, agents can now drive nested-Wayland desktops (Sway / Hyprland / niri / cage) end-to-end over MCP — switch the active Linux distro, screenshot the desktop, launch apps into it, and list its windows — the same control that already worked for X11 desktops.

• Android binaries run inside a proot guest no longer print a "failed to find generated linker configuration" warning. With "Expose Android system to guest" on, Haven now exposes Android's linker config file to the guest. Mounting the /linkerconfig folder is impossible — Android won't let the app look at it — but the file inside can be read, and that is enough. Nothing else changes.

• Connecting through a jump host (ssh -J) no longer stalls on the first attempt. The jump host connected, but the target's spinner span forever with no connection log — Haven opened the tunnelling channel before binding its streams, so the target's SSH banner was silently dropped and the connect waited for a greeting that never came again. Tapping a second time usually won the race; now the first tap works.

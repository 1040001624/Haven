<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!--
      BLE-serial console (Nordic UART Service / HM-10). Unlike Classic-SPP #406,
      a BLE UART peripheral (an nRF board, an HM-10 module, a BLE RS232 adapter)
      usually isn't bonded and won't appear in bondedDevices, so it must be
      DISCOVERED by an active BLE scan — hence BLUETOOTH_SCAN, which #406
      deliberately omitted. `neverForLocation` asserts we don't derive location
      from BLE, so no location permission is needed on API 31+. Pre-31, a BLE
      scan still legally requires (fine) location + the legacy BT permissions.
      BLUETOOTH_CONNECT covers the GATT connect and reading device names.
      Declared in the module that uses Bluetooth so the app inherits it via
      manifest merge (the #406 gap: the app doesn't depend on an unrelated
      module's manifest).
    -->
    <uses-permission
        android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission
        android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission
        android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />
    <uses-permission
        android:name="android.permission.ACCESS_FINE_LOCATION"
        android:maxSdkVersion="30" />
    <uses-feature
        android:name="android.hardware.bluetooth_le"
        android:required="false" />
</manifest>
